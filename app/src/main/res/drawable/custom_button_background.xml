<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Активное состояние -->
    <item android:state_enabled="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/blue" />
                    <!-- <stroke android:width="1dp" android:color="@color/blue" />  Белая обводка -->
                    <corners android:radius="15dp" /> <!-- Увеличен радиус для закругления -->
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Неактивное состояние -->
    <item android:state_enabled="false">
        <layer-list>
            <!--  <item android:drawable="@android:color/transparent"/> Фон кнопки -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/white" />
                    <stroke android:width="0.1dp" android:color="@color/black" /> <!-- Черная обводка -->
                    <corners android:radius="15dp" /> <!-- Увеличен радиус для закругления -->
                </shape>
            </item>
        </layer-list>
    </item>
</selector>